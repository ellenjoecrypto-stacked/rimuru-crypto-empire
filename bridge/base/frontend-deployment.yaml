#! frontend-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: frontend
    namespace: rimuru-empire
    labels:
        com.docker.compose.project: rimuru_empire
        com.docker.compose.service: frontend
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: rimuru_empire
            com.docker.compose.service: frontend
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: rimuru_empire
                com.docker.compose.service: frontend
                com.docker.compose.network.rimuru-network: "true"
        spec:
            restartPolicy: unless-stopped
            containers:
                - name: rimuru-dashboard
                  image: rimuru_empire-frontend
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: NODE_ENV
                      value: "production"
                    - name: REACT_APP_API_URL
                      value: "http://localhost:5000"
                  ports:
                    - name: frontend-80
                      containerPort: 80
