#! postgres-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: postgres
    namespace: rimuru-empire
    labels:
        com.docker.compose.project: rimuru_empire
        com.docker.compose.service: postgres
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: rimuru_empire
            com.docker.compose.service: postgres
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: rimuru_empire
                com.docker.compose.service: postgres
                com.docker.compose.network.rimuru-network: "true"
        spec:
            restartPolicy: unless-stopped
            containers:
                - name: rimuru-postgres
                  image: postgres:15-alpine
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: POSTGRES_DB
                      value: "rimuru_opportunities"
                    - name: POSTGRES_PASSWORD
                      value: "secure_password_here"
                    - name: POSTGRES_USER
                      value: "rimuru_user"
                  livenessProbe:
                    exec:
                        command:
                            - /bin/sh
                            - -c
                            - pg_isready -U rimuru_user
                    periodSeconds: 10
                    timeoutSeconds: 5
                    failureThreshold: 5
                  ports:
                    - name: postgres-5432
                      containerPort: 5432
                  volumeMounts:
                    - name: var-lib-postgresql-data
                      mountPath: /var/lib/postgresql/data
                    - name: docker-entrypoint-initdb-d-init-sql
                      mountPath: /docker-entrypoint-initdb.d/init.sql
            volumes:
                - name: var-lib-postgresql-data
                  persistentVolumeClaim:
                    claimName: postgres-postgres-data
                - name: docker-entrypoint-initdb-d-init-sql
                  hostPath:
                    path: C:\Users\Admin\OneDrive\Videos\rimuru_empire\init_db.sql
