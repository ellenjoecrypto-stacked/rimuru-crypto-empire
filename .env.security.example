# =============================================================
# RIMURU CRYPTO EMPIRE — .env.security template
# Copy this file to .env.security and fill in real values.
# NEVER commit .env.security to version control.
# =============================================================

# ── Database ─────────────────────────────────────────────────
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_USER=rimuru
POSTGRES_DB=rimuru_trading

# ── JWT / Auth ───────────────────────────────────────────────
JWT_SECRET_KEY=change_me_jwt_secret_min_32_chars

# ── Credential Vault ─────────────────────────────────────────
VAULT_MASTER_PASSWORD=change_me_vault_master_password

# ── Exchange API Keys ─────────────────────────────────────────
KRAKEN_API_KEY=
KRAKEN_SECRET_KEY=
COINBASE_API_KEY=
COINBASE_SECRET_KEY=
BINANCE_API_KEY=
BINANCE_SECRET_KEY=

# ── Ollama ────────────────────────────────────────────────────
# Override for Docker: OLLAMA_URL=http://ollama:11434
OLLAMA_URL=http://localhost:11434

# ── Paths (optional overrides) ───────────────────────────────
# CRYPTO_DB_PATH=data/crypto_findings.db
# SCAN_BASE_DIR=/home/user
# CDP_KEY_PATH=cdp_api_key.json
# RIMURU_SCAN_PATHS=/path/one,/path/two
# RIMURU_DATA_OUTPUT_DIR=data
